import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vihaan.thehumanarchive',
  appName: 'the-human-archive',
  webDir: 'public'
};

export default config;
